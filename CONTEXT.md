# Local Life Fulfillment

用户只描述生活目标，系统将目标转化为有供给证据、可授权、可履约的计划，并在现实变化时保留既有承诺。

## Language

**生活目标（Life Goal）**:
用户希望在现实生活中达成的结果；它不是门店搜索条件，也不是一组待填写的槽位。
_Avoid_: 查询、Prompt、需求文本

**目标契约（Goal Contract）**:
Agent 与用户对生活目标、上下文事实、约束、假设和开放问题的当前共同理解；锁定内容只能由用户改变。
_Avoid_: Intent、槽位集合

**意图帧（Intent Frame）**:
语义理解阶段向规划阶段交付的强类型结果，包含目标规格、处置方式、最小能力集合、关键不确定性与追问。
_Avoid_: 关键词分类、路由标签

**决策分支（Decision Branch）**:
一个追问选项对应的完整目标契约、能力集合、时间约束和已验证可行性；选择后可直接继续规划，不再把选项文案重新解释成意图。
_Avoid_: 快捷回复、按钮文案

**执行路径（Intent Path）**:
由意图帧按任务耦合度选择的决策拓扑；单一低耦合目标走快速路径直接交给一个 Scout，跨能力或高变动风险目标走编排路径并行取证后联合规划。
_Avoid_: 按关键词分支、固定垂类路由

**上下文事实（Context Fact）**:
目标契约中影响供给核验、规划或履约的场景语义事实，具有稳定语义键、用户可见名称和值来源。
_Avoid_: 通用槽位、垂类字段字典

**能力目录（Capability Catalog）**:
由供给 MCP 发布、用于描述当前可用能力、工具和证据语义的运行时契约；Scout 从目录动态生成。
_Avoid_: Agent 代码中的垂类列表、工具白名单常量

**供给（Supply）**:
在特定时间、价格和库存状态下可被履约的餐饮、活动、到店服务、即时配送或出行选项。
_Avoid_: POI、推荐内容

**供给证据（Supply Evidence）**:
证明某项供给在检查时刻具备价格、库存、时段或履约条件的事实。
_Avoid_: 推荐理由

**供给引用（Supply Reference）**:
一项供给从核验、报价、占位到承诺始终保持同一身份的生命周期引用。
_Avoid_: 临时 option id、订单号拼接

**报价（Quote）**:
供给方在有效期内给出的可支付总价与适用条件，不代表已经为用户保留库存。
_Avoid_: 预计价格、模型估价

**占位（Hold）**:
供给方在明确到期时刻前为当前任务临时保留的库存或时段。
_Avoid_: 缓存、已购买

**供给观察（Supply Observation）**:
对已核验、占位或承诺供给的库存、排队、价格、ETA 与履约状态变化的业务事实。
_Avoid_: 轮询结果、日志

**现实事件（Reality Event）**:
履约现场中由用户、环境或供给系统报告的变化；系统依据能力目录声明的可观察信号确定受影响节点，再尝试局部候补或重规划。
_Avoid_: 开发者场景开关、按垂类硬编码影响范围

**线下核验（Offline Verification）**:
通过 AI 外呼等线下渠道取得、可被计划引用并带有效期的供给证据。
_Avoid_: 模型猜测、商户备注

**计划图（Plan Graph）**:
为达成生活目标而形成的活动、约束、依赖、供给证据和履约状态的版本化图。
_Avoid_: 行程 JSON、推荐列表

**计划决策（Plan Decision）**:
模型对供给选项、开始时间和语义取舍做出的紧凑判断；领域 Module 随后从供给事实物化完整计划图。
_Avoid_: 模型生成的价格、库存、证据或授权对象

**可行方案集（Feasible Plan Set）**:
约束求解器从已核验供给中产生的硬约束可行组合，以及没有组合可行时的最小冲突原因。
_Avoid_: 候选列表、模型推荐

**Pareto 备选（Pareto Alternative）**:
在费用、时间、移动或体验维度上不被另一方案全面支配，并与主方案具有实质取舍的可行方案。
_Avoid_: 第二推荐、固定三个方案

**计划补丁（Plan Patch）**:
在新信息或异常发生后，对计划图做出的最小、可解释变更。
_Avoid_: 重新生成、整单覆盖

**计划策略（Plan Policy）**:
由当前主计划、少量 Pareto 备选和面对现实变化的条件切换策略组成的可执行承诺。
_Avoid_: 行程表、Plan Graph 别名

**决策点（Decision Point）**:
在最晚决策时刻前观察一个现实触发条件，并决定保持主计划或应用已验证候补的位置。
_Avoid_: 定时器、if 分支

**候补策略（Fallback Policy）**:
由最新供给证据支持、只替换受影响节点且明确授权影响的计划补丁模板。
_Avoid_: 模型临时推荐、兜底文案

**余量（Slack）**:
一个计划节点在不破坏后继硬约束的前提下能够消耗的时间。
_Avoid_: 缓存时间、安全系数

**承诺（Commitment）**:
用户已批准或外部系统已经接受、后续规划必须保留或显式补偿的计划节点。
_Avoid_: 已选项

**执行授权（Execution Mandate）**:
用户针对当前任务授予的预算、时间、品类和自动替换范围；支付、取消与退款仍需独立确认。
_Avoid_: 通用权限、永久授权

**履约命令（Fulfillment Command）**:
从已批准计划节点产生、要求外部世界发生变化的强类型动作。
_Avoid_: Tool Call、按钮事件

**履约事件（Fulfillment Event）**:
预约、购券、购票、配送下单、导航或叫车执行后产生的业务事实。
_Avoid_: 日志、Agent 消息

**现实完成（Realized Completion）**:
用户或供给观测确认计划节点已经在现实中发生；履约命令成功只建立承诺，不能替代现实完成。
_Avoid_: 预约成功、下单成功、接口成功

**完成证据（Completion Evidence）**:
由能力目录声明、能够证明一个承诺已经在现实中发生的供给状态、核销、到达或用户确认事实。
_Avoid_: 完成按钮、订单成功

**目标回执（Outcome Check-in）**:
现实步骤结束后，用户对生活目标是否达成的轻量反馈；它补充动作完成事实，但不是测评或评分。
_Avoid_: 满意度问卷、评测系统

**偏好事实（Preference Fact）**:
带主体、适用场景、来源、置信度与演化关系的结构化用户偏好证据。
_Avoid_: 聊天摘要、Memory 字符串

**偏好证据（Preference Evidence）**:
一次明确表达、实际选择、覆盖、取消或履约结果对偏好事实提供的新证据。
_Avoid_: 长期画像结论

**现场状态（Live State）**:
任务进入履约后，围绕当前下一步、现实风险、Agent 动作、等待事项与实际结果形成的消费者状态。
_Avoid_: 执行日志、完整计划复刻

**数字孪生（Supply Twin）**:
具备价格、库存、状态变化与订单行为的美团供给模拟世界。
_Avoid_: Mock 数据、静态 Fixture
