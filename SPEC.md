# Local Life Intent-to-Fulfillment Agent 产品规格

- 状态：Implemented v1，P0–P2 已实现并通过端到端验收；后续产品与实现的唯一事实源
- 更新时间：2026-08-20
- 产品名：好办（品牌承诺：你说想要，剩下好办）
- 产品形态：面向美团本地生活的单人 Intent-to-Fulfillment Agent
- 当前技术基线：Google ADK、DeepSeek V4 Flash、MCP、OR-Tools（拟引入）、Temporal
- 取代范围：所有已经删除或过期的产品、架构与交互设计文档

## 1. 产品定义

### 1.1 一句话定义

用户只描述希望达成的生活结果，系统负责形成目标共识、组合跨业务实时供给、解释关键取舍，并在用户授权后持续完成预约、购券、下单、导航与异常调整。

### 1.2 核心定位

本产品不是通用聊天助手，也不是自然语言搜索框，而是：

> 面向复杂本地生活目标的跨业务编排与动态履约 Agent。

[“意图到履约”已经是美团小团 2.0 的公开方向](https://www.meituan.com/news/NN260727198008607)，因此它本身不是差异化。产品差异必须来自以下能力：

1. 理解一个完整生活结果，而不是识别一个品类。
2. 在多个供给域之间处理时间、预算、地点与参与人约束。
3. 只在真正改变计划时追问用户。
4. 将计划表达为可修改、可授权、可执行的策略。
5. 在现实变化后保留已有承诺，只修复受影响部分。

### 1.3 核心产品承诺

> 一句话交代结果，Agent 把跨业务的一整件事办成，并在现场变化时继续把结果守住。

### 1.4 主要使用场景

优先服务同时具备以下至少两个特征的目标：

- 涉及两个及以上供给能力。
- 存在预算、时间、地点、人数或偏好冲突。
- 执行过程中存在库存、排队、交通、天气或用户迟到等变化。
- 用户更关心最终结果，不愿逐项搜索和比较。

首批重点场景：

- 约会：送花、晚餐、电影、交通。
- 朋友聚会：多人偏好、订位、娱乐、少走路、返程。
- 亲子半日活动：适龄、室内外、用餐、停车、结束时间。
- 下班后恢复：到店服务、清淡饮食、配送或返程。
- 商务接待：时间准确、环境要求、路线与预算。

单一、明确、低风险的请求仍然支持，但应走快速路径，不强制经过完整复杂规划。

## 2. 本期范围与非目标

### 2.1 本期范围

本 Spec 覆盖：

1. 产品定位与复杂任务分流。
2. 异步任务与实时交互。
3. 可编辑目标契约。
4. 基于反事实分支的高杠杆追问。
5. 约束求解辅助的可行方案空间。
6. 从 PlanGraph 到 PlanPolicy。
7. 用户与 Agent 共同编辑计划。
8. 任务内角色会话与结构化动态用户模型。
9. 供给能力从搜索深化为生命周期。
10. 履约阶段的现场生活伴侣。

### 2.2 明确非目标

- 不设计商业模式、广告、会员或收入方案。
- 不建设独立的可信度、合规或风控产品体系。
- 不处理多机房、多租户、极限吞吐等工程规模化问题。
- 不跨任务复用完整对话，不把 ADK 会话当作长期用户画像。
- 不建设独立测评平台或在线 Critic Agent。
- 不为其他模型保留抽象适配层。
- 不在本期继续增加酒店、机票、长途旅游或医疗决策能力。
- 不把多人协作设为必要前提；默认服务一名操作用户。

## 3. 产品原则

### 3.1 结果优先

生活目标是用户希望现实世界发生的结果，不是搜索词、品类或待填槽位。

### 3.2 任务是真实状态，聊天不是

任务、目标契约、计划、授权、供给证据和履约事件是持久状态。聊天只是修改这些状态的一种输入方式。

### 3.3 AI 负责语义判断，确定性模块负责严格约束

- 模型理解场合、情绪、模糊偏好、取舍和追问时机。
- 求解器处理时间、预算、容量、路线和依赖可行性。
- 领域 Module 物化价格、库存、动作和授权对象。
- Temporal 执行外部承诺、等待事件并驱动恢复。

### 3.4 不按关键词硬编码业务

能力、工具、上下文字段和生命周期由 MCP Capability Catalog 运行时发布。Agent 依据完整语义和目录选择能力，不在代码中维护关键词路由表。

### 3.5 候选不是义务

Scout 返回的供给只是证据。Planner 只选择实现用户目标所必需的最小承诺，不因为某个能力返回了候选就扩张目标。

### 3.6 最新责任时刻

不影响当前决策的信息延后收集。只有会改变能力集合、可行性、硬约束、目的地或授权的信息才阻塞当前步骤。

### 3.7 修改优先于重做

现实变化后优先产生最小 PlanPatch，已经完成或承诺的节点必须保留或显式补偿。

## 4. 目标端到端体验

### 4.1 用户旅程

```text
描述目标
  ↓ 立即创建任务
形成目标契约
  ↓ 必要时只追问一个关键分歧
并行核验供给
  ↓
求解可行组合
  ↓
解释推荐方案与关键取舍
  ↓ 用户可锁定、替换或修改
批准执行边界
  ↓
逐项确认交易
  ↓
现场履约与主动调整
  ↓
完成、取消、退款或局部恢复
```

### 4.2 产品界面阶段

产品界面分为四种模式，而不是始终停留在同一个工作台：

1. **目标模式**：表达结果、编辑目标契约、回答关键追问。
2. **决策模式**：查看推荐计划、取舍、备选并做局部修改。
3. **授权模式**：确认可自动执行范围和逐项交易。
4. **现场模式**：显示下一步、倒计时、履约状态、变化与调整动作。

产品本体是移动端交互，Web 只作为移动产品的演示载体：桌面浏览器中以手机宽度呈现完整产品，在窄屏设备上直接铺满视口。任务内使用单列滚动和底部“目标 / 方案 / 助手”导航，不使用桌面三栏工作台；授权、付款和现场动作必须落在拇指易触达区域。

### 4.3 任务状态

目标状态集合：

```text
understanding
clarifying
scouting
composing
awaiting_mandate
awaiting_transaction
executing
needs_replan
completed
unsupported
failed
```

`scouting` 与 `composing` 必须成为真实持久状态，使前端可以展示并行核验和组合过程。

## 5. 十项产品能力规格

## 5.1 能力一：聚焦复杂本地生活目标

### 目标

让产品在复杂跨业务任务上建立明确心智，而不是与美团搜索或普通推荐争夺所有查询。

### 功能要求

- IntentGovernor 必须判断任务属于快速路径还是编排路径。
- 判断依据是任务语义、能力数量、约束耦合和变化风险，不是关键词或品类表。
- 快速路径可直接调用一个 Scout 并返回可执行选项。
- 编排路径必须形成目标契约、并行 Scout、可行组合和计划策略。
- 首页文案以“把一整件事办成”为核心，不枚举技术框架。
- DeepSeek、ADK、MCP、Temporal 等技术信息只出现在开发者抽屉。

### 验收标准

- “今晚 19:00 两个人吃日料”走单能力快速路径。
- “送花、晚餐、电影且 23:00 前结束”走复杂编排路径。
- 两种路径共用同一个语义入口，前端不要求用户选择业务品类。
- 路由新增能力时不修改 IntentGovernor 的业务关键词代码。

## 5.2 能力二：异步任务与实时过程交互

### 目标

用户提交后立即看到系统形成目标和核验供给的真实进展，不等待完整推理结束。

### 功能要求

- `POST /api/tasks` 创建并持久化任务后立即返回 task id 和初始快照。
- Intent、Scout 和 Planner 在后台推进任务。
- SSE 持续发送状态变化和语义进度事件。
- 进度事件只展示可验证的工作状态，不展示隐藏思维链。
- 用户在 Agent 工作期间可以补充或修改目标。
- 新输入到达时，系统取消尚未形成外部承诺的旧决策轮，并基于最新 revision 重新推进。
- 用户可显式停止当前规划任务。

### 可展示进度

- 已识别目标、预算、时间与人数。
- 正在核验哪些供给能力。
- 哪些能力已经返回候选。
- 是否发现预算、时间或库存冲突。
- 正在比较可行组合或生成计划补丁。

### 验收标准

- 提交目标后，前端无需等待模型完成即可进入任务页。
- 首个任务状态在创建响应中可用。
- 同一任务的事件顺序与 task revision 一致。
- 用户修改目标后，旧轮结果不能覆盖新轮状态。

## 5.3 能力三：可编辑目标契约

### 目标

让用户校正 Agent 的理解，而不是通过多轮聊天重新描述全部需求。

### 领域对象

`GoalSpec` 演进为 `GoalContract`，包含：

- `outcome`：希望达成的结果。
- `context_facts`：地点、参与人、场合、时间窗口、行动限制等语义事实。
- `constraints`：硬约束与软约束。
- `preferences`：本次任务适用的偏好。
- `assumptions`：可编辑的系统假设。
- `open_questions`：仍可能影响决策的信息。
- `locked_fields`：用户明确要求后续规划不得改变的部分。

字段名称与可用上下文类型由核心语义模型和 Capability Catalog 共同约束，不为每个供给域在页面代码中维护一组固定槽位。

### 交互要求

- 预算、最晚完成时间、地点、人数可以直接编辑。
- 每个约束可在“必须”与“尽量”之间切换。
- 用户可以删除、修改或确认假设。
- 用户可以锁定已经确认的事实。
- `deadline` 根据语义显示为“最晚结束”“最晚送达”或“最晚到达”，不能固定显示为“到家时间”。
- 编辑结果统一产生新的 task revision，并触发受影响部分重新规划。

### 验收标准

- 用户将预算从 500 改为 400 后，不需要重新描述其他条件。
- 用户锁定晚餐后修改电影时间，晚餐节点保持不变。
- 删除默认地点假设后，系统在地点会改变供给时发起追问。
- 前端展示值、Planner 约束和最终授权使用同一份 GoalContract。

## 5.4 能力四：基于计划分叉的高杠杆追问

### 目标

Agent 只在缺失信息会实质改变结果时追问，并让用户理解不同答案的影响。

### 决策方式

1. IntentGovernor 识别一个关键不确定性。
2. 为 2–4 个合理答案形成轻量反事实分支。
3. 比较各分支的能力集合、可行性、硬约束和主要计划结构。
4. 只有分支结果实质不同时才向用户追问。
5. 若分支收敛到同一方案，则采用可编辑假设继续。

### 追问格式

- 一次只问一个问题。
- 提供 2–4 个有实质差异的选项。
- 每个选项说明它会如何改变方案。
- 始终允许自由文本回答。
- 不询问 Scout 可以从供给系统获取的商家事实。

### 验收标准

- “今晚想放松”会询问恢复型还是体验型，因为能力集合不同。
- 缺少精确门牌但商圈足够搜索时，不阻塞供给比较。
- 两种答案得到同一供给与计划结构时不追问。
- 追问选项不是业务分类菜单，而是用户目标层面的选择。

## 5.5 能力五：约束求解辅助的可行方案空间

### 目标

把模型从算术、排程和组合爆炸中解放出来，让它集中处理语义偏好和情境取舍。

### 处理链路

```text
IntentGovernor
  → GoalContract 与语义目标
并行 Scouts
  → Grounded Candidate Sets
OR-Tools / CP-SAT
  → Feasible Plan Set 与 Pareto Frontier
LifeGoalPlanner
  → 推荐方案、关键取舍与少量备选
PlanningModule
  → 物化与最终不变量校验
```

### 求解器负责

- 时间窗口与节点持续时间。
- 前后依赖和移动耗时。
- 总预算和逐项金额。
- 人数与容量。
- 营业、场次、预约与送达窗口。
- 硬约束可行性。
- 生成不被其他方案全面支配的候选组合。

### 模型负责

- 将“安静”“别太赶”“第一次约会”等语言转为语义目标。
- 判断软约束的重要性和适用上下文。
- 从 Pareto 候选中选择最符合目标的方案。
- 解释为什么推荐、牺牲了什么，以及哪个备选有不同取舍。

### 禁止做法

- 不在代码中写死每个场景的评分权重。
- 不让模型生成价格、库存、时长或证据。
- 不为了展示选择而固定返回三个方案。

### 验收标准

- 所有推荐方案在进入 Planner 解释前已经满足硬约束。
- 没有可行解时返回造成不可行的最小约束集合。
- 有实质取舍时最多展示一个推荐方案和两个备选方向。
- 备选必须在价格、时间、移动或体验目标上具有可解释差异。

## 5.6 能力六：从 PlanGraph 升级为 PlanPolicy

### 目标

让 Agent 交付的不是静态时间表，而是一套面对现实变化仍能守住目标的执行策略。

### 新领域对象

`PlanPolicy` 包含：

- `primary_plan`：当前推荐 PlanGraph。
- `decision_points`：需要在特定时间或事件发生时判断的节点。
- `triggers`：库存、排队、天气、ETA、价格、用户位置或履约事件条件。
- `fallbacks`：经过供给验证的候补节点或计划补丁模板。
- `slack`：节点允许消耗的时间余量。
- `decision_deadline`：必须在何时前决定是否切换。
- `authorization_effect`：当前 Mandate 是否允许自动切换。

### 行为要求

- 主计划仍然保持紧凑，不把所有可能分支同时展示给用户。
- 触发条件发生后优先应用最小 PlanPatch。
- 已完成、执行中或不可撤销节点不得被无声替换。
- fallback 必须来自最新 Scout 证据或重新核验，不得由模型凭空生成。
- 无可行 fallback 时向用户说明哪个目标已经无法同时满足。

### 验收标准

- 餐厅排队超过可用 slack 时只替换晚餐及其受影响依赖。
- 下雨导致步行不可接受时可以在 Mandate 范围内切换出行方案。
- 电影售罄时保留已完成晚餐，并重新计算后续节点。
- 每次切换都产生版本化 PlanPatch。

## 5.7 能力七：用户与 Agent 共同编辑计划

### 目标

让用户通过自然语言和直接操作修改同一份计划，而不是接受或推翻整个结果。

### 支持操作

- 锁定节点。
- 替换节点。
- 删除非必要节点。
- 调整开始时间或预算。
- 指定“其他部分不变”。
- 查看一个节点的少量候选。
- 选择更便宜、省路、宽松或体验更强的替代方向。
- 撤销最近一次未履约修改。

### 统一语义

自然语言和 UI 操作都转换为同一个 `PlanEditIntent`，随后由 Agent 和 PlanningModule 产生 `PlanPatch`。前端不得直接拼接完整 PlanGraph。

### 验收标准

- “晚餐不变，只换电影”只修改电影及其后继依赖。
- 点击锁定与自然语言说“这个别动”产生相同领域状态。
- 用户拖动电影时间后，系统自动修复路线和返程，不重选无关供给。
- 修改超过预算或授权范围时回到决策或授权阶段。

## 5.8 能力八：任务内会话复用与动态用户模型

### 目标

在同一任务中复用各角色的 ADK 会话与已经核验的供给证据，避免重复理解、重复检索和重复解释；跨任务理解仍依赖结构化 PreferenceFact，而不是回放长对话历史或聊天摘要。

### 领域对象

`PreferenceFact` 至少包含：

- `subject`：用户本人、同行人或关系场景。
- `context_scope`：独处、约会、亲子、朋友聚会等适用范围。
- `preference`：结构化偏好内容。
- `source`：明确表达、实际选择、覆盖 Agent、取消或履约结果。
- `confidence`：当前可信程度。
- `observed_at`：最近证据时间。
- `valid_from` / `supersedes`：偏好变化关系。
- `task_id`：产生事实的任务来源。

### 记忆规则

- 每个任务为 Intent、Scouts、追问协调和 Planner 建立稳定的角色会话；任务内后续修订继续使用对应会话。
- 用户选择结构化 Decision Branch 后，直接复用该分支已核验的供给证据，不重新解释选项文案，也不重复发起相同 Scout 调用。
- 任务结束后不把完整 ADK 会话作为跨任务记忆；跨任务只读取与当前目标相关的少量 PreferenceFact。
- 明确表达优先于行为推断。
- 本次场景偏好不能自动升级为全局偏好。
- 新事实可以覆盖或缩小旧事实的适用范围。
- 只检索当前目标相关的少量 PreferenceFact。
- 用户可以查看、修改和删除结构化偏好。

### 验收标准

- 用户独处时追求快捷，不影响约会时的安静偏好。
- 带孩子时的少走路要求不会被应用到所有任务。
- 用户多次替换过辣餐厅后可以形成饮食偏好候选，但需要保持来源信息。
- 新任务使用持久偏好，但创建新的任务级 ADK 角色会话；旧任务聊天不会被回放。

## 5.9 能力九：供给从搜索深化为生命周期

### 目标

让每个供给域不仅能返回候选，还能完整参与计划、占位、交易、观察和恢复。

### 统一生命周期

```text
discover
  → search
  → verify
  → quote
  → hold
  → commit
  → observe
  → change / cancel / refund
```

### Capability Catalog 扩展

每项能力运行时发布：

- 适用结果语义。
- 可用工具与输入输出 schema。
- 支持的生命周期阶段。
- 可观察事件类型。
- 可取消、改期和退款动作。
- 占位有效期与交易前刷新要求。
- 该能力需要的上下文字段 schema。

### 首批深化范围

优先深化餐饮、休闲娱乐和出行，暂不以增加更多能力域为目标。

餐饮重点：

- 桌位、排队、包间、营业状态、套餐、优惠券、环境与人数适配。

休闲娱乐重点：

- 场次、座位、适龄、票种、余票、入场规则、改退条件。

出行重点：

- 路线、步行量、实时 ETA、叫车报价、多站点和到达约束。

跨域重点：

- 同商圈与真实步行路径。
- 优惠与总预算组合。
- hold 过期后的刷新与局部替换。
- 无线上预约商家通过 AI 外呼形成可引用的供给证据。

### 验收标准

- Planner 只引用已验证或已占位的 option id。
- quote、hold 和 commit 使用同一供给身份，不在阶段间丢失选项语义。
- hold 过期会触发刷新或 PlanPatch。
- 取消和退款作为正常生命周期动作，而不是异常脚本。

## 5.10 能力十：履约阶段的现场生活伴侣

### 目标

付款或预约后，产品从规划工具切换为帮助用户守住结果的现场伴侣。

### 现场界面必须展示

- 当前最重要的下一步。
- 距离出发、预约、送达或入场的时间。
- 订单、预约、票券、车辆与导航状态。
- 哪个变化可能影响后续节点。
- Agent 已经自动做了什么、正在等待什么。
- 可用的改期、取消、退款和替代动作。

### 主动行为

- 在出发前重新核验路线和供给状态。
- 用户迟到时重新计算后续可行性。
- ETA、排队、天气或订单状态变化时评估是否需要 PlanPatch。
- 仅在必须由用户做决定时发送高杠杆通知。
- 任务结束后归档实际费用、履约结果和用户主动修改。

### 验收标准

- 用户打开执行中任务时首先看到下一步，而不是完整技术 trace。
- 迟到不会直接重做整张计划，而是先计算 slack 和受影响后继节点。
- 履约完成后展示实际结果，不继续显示预计价格和预计时间。
- 取消、退款或改期完成后，任务状态与计划节点同步更新。
- 预约、下单或出票成功只表示承诺已经建立；只有现实完成信号才能完成对应节点与任务。
- 已完成或已取消的任务不再展示取消、退款、改期等进行中操作。

## 6. 领域模型演进

| 当前对象 | 目标对象 | 变化 |
|---|---|---|
| `GoalSpec` | `GoalContract` | 增加上下文事实、开放问题、锁定字段和统一编辑语义 |
| `IntentFrame` | 保留 | 输出快速/编排路径、目标契约与关键不确定性 |
| `ScoutReport` | 保留并扩展 | 增加 quote、hold、风险、事件订阅和生命周期信息 |
| `PlanDecision` | 保留并收窄 | 只判断候选组合、取舍和用户解释 |
| `PlanGraph` | `PlanPolicy.primary_plan` | 仍表示当前确定执行路径 |
| `PlanPatch` | 保留并深化 | 支持触发来源、锁定节点和 authorization effect |
| 无 | `FeasiblePlanSet` | 求解器产生的可行组合与不可行原因 |
| 无 | `DecisionPoint` | 执行中的条件判断与最晚决策时刻 |
| 无 | `FallbackPolicy` | 已验证候补和允许的最小替换 |
| `MemoryUpdate` | `PreferenceFact` | 结构化、带场景和时间演化的用户事实 |
| `FulfillmentEvent` | 保留并扩展 | 增加 ETA、排队、hold 过期、改期和退款事件 |
| 无 | `PlanEditIntent` | 统一自然语言与直接 UI 编辑 |

所有领域对象继续采用强类型 schema，模型只输出语义判断，不输出可由供给或领域 Module 决定的事实。

## 7. AI 编排职责

### 7.1 目标编排

```text
用户输入 / UI 编辑 / 现实事件
            ↓
      IntentGovernor
            ↓
 GoalContract + 路径决策
            ↓
   动态 ADK Scout Workflow
            ↓
    Grounded Candidate Sets
            ↓
     OR-Tools Feasibility
            ↓
       LifeGoalPlanner
            ↓
 PlanPolicy / Clarification / Inform
            ↓
  PlanningModule 物化与校验
```

### 7.2 动态而非硬编码

- Scout 集合从 Capability Catalog 动态生成。
- MCP 发布能力上下文 schema 和生命周期，不在 Agent Prompt 中枚举业务字段。
- 求解目标从 GoalContract 与候选属性生成，不按案例名称选择固定算法。
- 简单路径和复杂路径由语义决策产生，不按关键词判断。

### 7.3 不增加在线 Critic

本期不增加评审 Agent 或独立在线测评链路。现有修复策略保持：

1. 模型生成语义决策。
2. 领域 Module 做确定性校验。
3. 仅在具体不变量失败时让同一 Planner 修复一次。
4. 仍失败则返回可解释冲突，不无限自我审查。

## 8. 交互信息架构

### 8.1 消费者默认视图

只展示：

- 目标契约。
- 当前 Agent 状态。
- 推荐计划与关键取舍。
- 需要用户做出的一个决定。
- 授权、交易和履约动作。
- 现实变化及其影响。

消费者视图遵循移动优先约束：

- 同一时刻只有一个主视图，避免把目标、计划和交互并排压缩。
- 底部导航切换目标、方案和助手，出现关键追问或待确认动作时提供明确注意状态。
- 授权和交易使用底部行动卡，主按钮不被滚动内容带走。
- 明亮背景与高对比正文作为默认主题，不提供暗色工作台式主界面。
- Web 宽屏只增加演示说明和手机外框，不改变移动端产品的信息架构。

### 8.2 开发者抽屉

仅调试模式展示：

- Agent 名称与阶段。
- MCP 工具调用。
- 模型、框架与延迟。
- world version 与原始 trace。
- schema 验证或修复信息。

消费者主界面不展示 “DeepSeek V4 在线”“Google ADK”“MCP Supply”等实现标签。

## 9. 分阶段落地

### P0：从 Demo 变成可交互产品

对应能力：1、2、3、7。

- 异步创建 task，后台推进，SSE 展示真实阶段。
- 增加 `scouting` 和 `composing` 状态。
- GoalContract 可编辑、可锁定。
- 自然语言和直接操作统一为 PlanEditIntent。
- 支持节点锁定与局部替换。
- 消费者视图隐藏技术实现信息。
- 快速路径与复杂编排路径共用入口。

完成标志：用户从提交目标到批准方案的全过程都可见、可改、不会因修改一个条件而重做无关部分。

### P1：增加规划智能深度

对应能力：4、5、6。

- 引入 OR-Tools / CP-SAT。
- 形成 FeasiblePlanSet 与不可行原因。
- 用真实计划分叉决定是否追问。
- 推荐方案加少量 Pareto 备选。
- 引入 PlanPolicy、DecisionPoint、slack 和 fallback。
- 现实变化驱动最小 PlanPatch。

完成标志：复杂跨域方案先保证可行，再由模型做有上下文的取舍；追问和重规划都能解释其必要性。

### P2：形成持续服务能力

对应能力：8、9、10。

- PreferenceFact 动态用户模型。
- 深化餐饮、娱乐和出行生命周期。
- 支持 quote、hold、commit、observe、change、cancel、refund。
- 接入 AI 外呼作为线下供给 adapter。
- 上线现场模式和主动重规划。
- 任务完成后沉淀实际结果与偏好事实。

完成标志：Agent 不仅生成计划，还能从任务开始一直服务到现实结果完成。

## 10. 产品成功标准

这些是产品行为指标，不建设独立测评系统。

- **首次有用状态时间**：提交后多久看到目标理解或供给核验进展。
- **目标契约修正率**：用户需要修改多少系统理解。
- **追问有效率**：回答后是否真的改变能力集合或计划。
- **方案一次接受率**：用户不修改即批准的比例。
- **局部修改率**：修改是否保持在用户要求的节点和依赖范围内。
- **计划完成率**：进入执行后最终达成生活目标的比例。
- **异常恢复率**：发生现实变化后仍完成目标的比例。
- **无效操作数**：用户为了达成目标仍需手工搜索、切换或重复输入的次数。

北极星结果：

> 用户不需要自己重新拆解任务，Agent 在最少必要交互下完成了原始生活目标。

## 11. 必须覆盖的产品验收场景

### 11.1 快速单域

“今晚 19:00 望京两个人安静吃日料，预算 500。”

- 走快速路径。
- 不询问 Scout 可以发现的餐位信息。
- 返回可预约选项和必要交易动作。

### 11.2 跨域约会

“18:00 前送花，19:00 吃饭，20:45 看电影，23:00 前结束。”

- 并行核验配送、餐饮、电影和必要路线。
- 求解器保证时间和预算可行。
- 用户可锁定晚餐后只更换电影。

### 11.3 模糊目标

“今晚下班后想放松一下。”

- 只追问一个会改变计划的问题。
- 选项描述结果差异，不展示业务分类菜单。

### 11.4 目标契约修正

用户将“23:00 前到家”修改为“23:00 前活动结束”。

- deadline 语义和 UI 标签同时变化。
- 只重新规划受影响节点。

### 11.5 无可行方案

“20:45 看 118 分钟电影，但必须 22:20 前结束。”

- 返回造成不可行的核心约束。
- 不生成明知违反时间的计划。

### 11.6 现实变化

晚餐已完成，电影突然售罄。

- 保留晚餐事实。
- 只重选电影及受影响后继节点。
- 必要时提出放松哪个约束。

### 11.7 用户迟到

用户比计划晚到 25 分钟。

- 先消耗 slack。
- 判断哪些后继节点仍可行。
- 不直接重新生成整张计划。

### 11.8 动态偏好

用户独处时偏好快捷，约会时偏好安静。

- 两个 PreferenceFact 有不同 context scope。
- 当前任务只应用相关偏好。
- 不跨任务复用历史聊天会话；同一任务内复用角色会话和已核验供给证据。

## 12. 实施约束

- 只使用当前配置的 DeepSeek V4 Flash，不建设模型 adapter 层。
- 使用 Google ADK Workflow 编排 Agent，不自研 Agent runtime。
- 使用 OR-Tools / CP-SAT 做约束求解，不自研通用求解器。
- 使用 MCP 作为供给能力发布和工具调用协议。
- 使用 Temporal 处理有外部状态的长程履约。
- 使用 Postgres 持久化任务、偏好和供给世界状态。
- 保持 task revision CAS；同一 task 修改必须串行或显式冲突。
- 不添加与真实当前需求无关的兼容层、迁移框架或防御脚手架。

## 13. Definition of Done

本 Spec 的产品目标全部完成，需要同时满足：

1. 用户提交后立即进入可观察任务，而不是等待完整 Agent 返回。
2. GoalContract 可以直接编辑、锁定并驱动局部重规划。
3. 追问由真实计划分叉决定，而不是固定槽位缺失。
4. 跨域计划经过求解器可行性处理后再交给模型取舍。
5. Agent 输出包含主计划、必要 decision points 和已验证 fallback。
6. 自然语言与 UI 编辑统一产生 PlanPatch。
7. 同一任务复用角色会话与分支供给证据；新任务只继承结构化 PreferenceFact，不回放旧任务对话。
8. 三个重点供给域具备完整生命周期，而不止 search/get。
9. 执行阶段有独立现场模式，并能根据现实事件局部恢复。
10. 用户从目标表达到现实完成不需要退回手工搜索来重新拆解任务。
