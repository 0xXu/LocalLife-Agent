# 周末管家：周末半日活动执行助手

## 1. 赛题理解

本题的重点不是“附近有什么”，而是“帮用户把事情做完”。周末管家将用户一句自然语言目标转成可执行的本地生活计划：解析时间、人群、距离、饮食和预算约束，检索活动与餐厅，生成半日时间轴，并在用户确认后完成模拟预约、订座和发送计划。

核心差异是从推荐列表升级为执行闭环：

```text
一句话输入 -> 约束解析 -> 模拟工具调用 -> 时间轴/路线 -> 用户确认 -> 执行回执 -> 失败恢复
```

## 2. P0 演示范围

当前提交选择稳定优先的 P0：确定性状态机 + 模拟工具 + 可展开规划过程。真实地图、真实支付、真实订座和复杂多助手不作为主路径，避免现场演示受外部接口影响。

演示覆盖家庭半日场景：用户想和妻子、5 岁孩子下午出门，不想太远，妻子在减脂。系统生成亲子活动、低脂餐厅和饭后散步安排，并展示路线概览、预算和执行动作。

## 3. 工具链设计

| 工具 | 作用 | 演示表现 |
|---|---|---|
| `parse_user_goal` | 从自然语言抽取人群、时间、饮食、距离 | 约束卡片 |
| `search_places` | 检索亲子活动、散步点、甜品点 | 规划过程 |
| `search_restaurants` | 检索低脂、儿童友好餐厅 | 规划过程 |
| `rank_candidates` | 按距离、儿童友好、饮食、等待时间排序 | 推荐理由 |
| `optimize_route` | 生成顺路时间轴 | 路线概览 |
| `check_availability` | 检查模拟可订性 | 规划过程 |
| `create_reservation` | 用户确认后生成预约/订座回执 | `TKT-*`、`RES-*` |
| `send_plan_message` | 将计划发送给家人或朋友 | `MSG-*` |

所有工具都有明确输入输出和可见日志。副作用工具只在用户点击确认后执行。

## 4. 异常与恢复

演示内置“餐厅无位”异常：当主餐厅不可订时，系统保留亲子活动和饭后安排，只替换餐厅节点，并展示差异：

- 替换前后餐厅名称
- 失败原因
- 预算变化
- 路线变化
- 被保留的行程节点

这证明系统不是生成一次性攻略，而是在计划失败时能局部恢复。

## 5. 安全与可信

- 地点来自本地模拟 POI 数据，不允许模型自由编造。
- 订座、预约、发送消息属于副作用动作，必须由用户确认。
- 回执必须显示 ID 和状态，不允许假装真实交易已发生。
- 演示不保存真实手机号、支付信息、精确住址或联系人隐私。

## 6. 评分点映射

| 官方维度 | 对应设计 |
|---|---|
| 创新性 | 从推荐升级为本地生活执行助手，突出失败恢复 |
| 完整性 | 输入、解析、检索、排序、路线、确认、回执、恢复全链路 |
| 应用效果 | 中文对话式界面展示约束、规划过程、时间轴、路线和执行结果 |
| 商业价值 | 串联餐厅订座、活动预约、到店消费和计划分享 |

## 7. 演示方式

打开 `index.html`，依次点击“生成计划”“查看规划过程”“确认执行”“换一家餐厅”。评委能在同一屏看到系统如何理解目标、调用工具、生成计划、执行动作并从失败中恢复。
